apiVersion: apps/v1
kind: Deployment
metadata:
  name: retrieval
  namespace: research
spec:
  replicas: 1
  selector:
    matchLabels: { app: retrieval }
  template:
    metadata:
      labels: { app: retrieval }
    spec:
      containers:
        - name: retrieval
          image: research/retrieval:local
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef: { name: app-config }
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: retrieval
  namespace: research
spec:
  selector: { app: retrieval }
  ports:
    - port: 8000
      targetPort: 8000
