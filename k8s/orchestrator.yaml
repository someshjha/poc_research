apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: research
spec:
  replicas: 1
  selector:
    matchLabels: { app: orchestrator }
  template:
    metadata:
      labels: { app: orchestrator }
    spec:
      containers:
        - name: orchestrator
          image: research/orchestrator:local
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef: { name: app-config }
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet: { path: /healthz, port: 8000 }
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: research
spec:
  selector: { app: orchestrator }
  ports:
    - port: 8000
      targetPort: 8000
